import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(theme => ({
  root: {
    // width: '500px',
    // height: '500px'
  },
  canvasContent: {
    overflow: 'auto',
    position: 'relative'
  }
}));
